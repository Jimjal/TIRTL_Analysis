# validate_latest — Validator 快捷验收（template）

- 读取：ACTIVE_WORKFLOW / CHAT_PROTOCOL / BASE / Validator role
- 确保 Required Skills（如有）
- 定位最新 Promptbook → 按 AC 全量验收 → 验收总结追加（含证据/skills）→ FAIL 打回（REJECT_WITH_FIX）或 PASS（VALIDATION_PASS + 可发 CLOSE_REQUEST）
